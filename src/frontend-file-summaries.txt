{
  "frontend/src/index.css": "Global styles with emerald and amber OKLCH color theme for light and dark modes, avoiding blue/purple palette.",
  "frontend/tailwind.config.js": "Tailwind configuration with custom emerald/amber color tokens, Inter font family, and enhanced shadows for the design system.",
  "frontend/src/App.tsx": "Main application component that renders the DataEntryPage and Toaster for notifications.",
  "frontend/src/utils/validation.ts": "Reusable validation functions for required fields, mobile number (digits-only, 10-15 length), and amount (>0) with typed error results.",
  "frontend/src/features/entries/queries.ts": "React Query hooks for creating, listing, updating, and deleting entries with enhanced authorization error detection, proper retry logic, backend-provided error messages, cache invalidation on success, and exported ENTRIES_QUERY_KEY for external invalidation.",
  "frontend/src/components/EntriesTable.tsx": "Presentational table component for displaying saved entries with formatted dates, amounts, timestamps, days-since calculation (with column header \"DAYS\"), and Edit/Delete actions with modal dialogs for editing entries and confirmation for deletion, including proper validation and loading states.",
  "frontend/src/pages/DataEntryPage.tsx": "Main data entry page with Internet Identity authentication, auth-gated form submission, entries table with error handling, analytics section, and Import/Export card with Excel-compatible CSV export, browser-based PDF export, and CSV/XLSX import with batch processing and summary toasts.",
  "frontend/src/utils/date.ts": "Date utility functions for parsing YYYY-MM-DD manual dates in local time and calculating whole-number day differences (including negative values for future dates), returning null for invalid dates.",
  "frontend/src/utils/analytics.ts": "Frontend aggregation helpers that transform saved entries into monthly totals/counts for a selected year (Jan-Dec) and yearly totals/counts for all years, using manualDate parsing and skipping invalid dates.",
  "frontend/src/components/AnalyticsSection.tsx": "Analytics section UI with Monthly/Yearly toggle, year selector for monthly view, and colored bar charts (using emerald/warm theme) displaying total amount and entry count with clear labels and English empty-state message.",
  "frontend/src/utils/authErrors.ts": "Utility functions for detecting authorization errors and providing user-friendly English sign-in prompts for different contexts (create, view, update, delete entries).",
  "frontend/src/components/AuthStatusBar.tsx": "Reusable authentication status component that displays current sign-in state with visual indicators and provides Sign in/Sign out buttons using Internet Identity.",
  "frontend/src/utils/entriesImportExport.ts": "Utility module for CSV and text file export with shared column definitions, row formatting helpers (including DAYS calculation and timestamp formatting), and client-side file generation using native browser APIs without external dependencies.",
  "frontend/src/utils/entriesXlsxImport.ts": "CSV/XLSX import parser that reads files as text, parses CSV format with proper quote handling, maps flexible column headers to entry fields, validates rows using existing validation functions, and returns parsed valid rows plus per-row error details.",
  "frontend/package.json": "Package configuration with dependencies including SheetJS (xlsx) for spreadsheet manipulation and jsPDF with jspdf-autotable for PDF generation, plus all existing React, Radix UI, and ICP SDK dependencies.",
  "frontend/src/types/xlsx.d.ts": "TypeScript type declarations for the xlsx (SheetJS) library, defining interfaces for workbooks, worksheets, cell objects, column info, and utility functions for reading and writing spreadsheet files.",
  "frontend/src/types/jspdf.d.ts": "TypeScript type declarations for jsPDF and jspdf-autotable libraries, defining the jsPDF class with text, font, page methods and autoTable plugin interface for PDF table generation.",
  "frontend/src/utils/entriesCSVImport.ts": "CSV import parser that reads files using FileReader, parses CSV with proper quote handling, maps flexible column headers to entry fields, validates rows using existing validation functions, and returns parsed valid rows plus per-row error details.",
  "frontend/src/utils/entriesXlsxExport.ts": "Excel-compatible CSV export utility that generates CSV files with UTF-8 BOM for Excel compatibility, using the same column structure as the original XLSX requirement without external dependencies.",
  "frontend/src/utils/entriesPdfExport.ts": "Browser-based PDF export utility that opens a printable HTML page in a new window, allowing users to save as PDF using the browser's native print dialog, styled with emerald theme colors."
}